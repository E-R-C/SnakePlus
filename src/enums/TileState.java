package enums;

public enum TileState {
	EMPTY,
	SNAKE,
	SNAKE_HEAD,
	POWERED_SNAKE,
	POWERED_SNAKE_HEAD,
	FOOD,
	BORDER, 
	OBSTACLE,
	POWER_UP, GAME_OVER
}

/* Maybe associate colors with each possible state in this class? */